# AI Workflow - Мастер Темплејт за Математички Учебници

## 🎯 Цел
Овој темплејт е наменет за **LLM системи** (GPT-4, Claude, Gemini) за да генерираат код за математички визуелизации од задачи и слики.

---

## 📋 Работен Тек (Workflow)

### 1. Корисникот дава задача
Корисникот може да даде:
- 📸 **Слика** од учебник со задачи
- 📄 **PDF** со лекции/задачи  
- 📝 **Текстуална задача** на македонски/англиски/руски/турски/

### 2. AI анализира и одлучува
AI (ти) треба да:

1. **Прочитај ја задачата**
2. **Одлучи која алатка да користиш:**
   - 🔷 **Asymptote** → Статички математички дијаграми (геометрија, графици, координатни системи)
   - 🎬 **Manim** → Анимации (за објаснување на концепти)
   - 🖼️ **Image Gen** → Илустрации, декоративни елементи
3. **Генерирај код** според соодветниот темплејт
4. **Зачувај го** во правилната структура

### 3. Процесирање
- Код се извршува локално преку Python скрипти
- Генерираат се PDF/SVG/MP4 фајлови
- Се организираат во структура на лекции

---

## 🔷 Asymptote - За Статички Дијаграми

### Кога да користиш Asymptote?
- ✅ Геометриски фигури (триаголници, кругови, правоаголници)
- ✅ Координатни системи и графици
- ✅ Броjna оска (number line)
- ✅ Математички дијаграми со лабели
- ✅ Статички визуелизации

### Темплејт - ЗАДОЛЖИТЕЛНО ЗА МАКЕДОНСКИ ТЕКСТ

```asymptote
// ЗАДОЛЖИТЕЛЕН Header
settings.tex="pdflatex";
settings.outformat="pdf";
texpreamble("\usepackage[T2A]{fontenc}");
texpreamble("\usepackage[utf8]{inputenc}");
texpreamble("\usepackage[russian]{babel}");
texpreamble("\usepackage{lmodern}");

// Големина
size(400, 300);

// ТВОЈОТ КОД
// Пример:
draw(circle((0,0), 2), blue+1.5);
label("Круг", (0, -2.5), fontsize(12pt));
```

### Важни правила:
1. **СЕКОГАШ** користи го header-от за кирилични букви
2. **Бои**: Користи појаки бои за SVG (linewidth 1.5-2)
3. **Фонтови**: fontsize(10pt), fontsize(12pt), fontsize(14pt)
4. **Лабели**: `label("Текст", (x,y), позиција)`
5. **Математика**: `label("$x^2 + y = 4$", (x,y))`

### Генерирање и извршување

**За PDF (Word/LaTeX):**
```python
# Зачувај како: grade_XX/YY_naziv.asy
# Потоа изврши:
python render.py grade_XX/YY_naziv.asy
```

**За SVG (Illustrator/Web):**
```python
# Автоматска конверзија PDF → SVG со Create Outlines
python render_svg.py grade_XX/YY_naziv.asy
```

📖 **Детали**: Погледни `ASYMPTOTE_AI_TEMPLATE.md`

---

## 🎬 Manim - За Анимации

### Кога да користиш Manim?
- ✅ Анимирани објаснувања (како нешто се движи/трансформира)
- ✅ Постепено градење на дијаграми
- ✅ Визуелно објаснување на концепти
- ✅ Видео лекции

### Темплејт

```python
from manim import *

class MojaDemonstracija(Scene):
    def construct(self):
        # Твој код тука
        circle = Circle(radius=2, color=BLUE)
        self.play(Create(circle))
        self.wait()
```

### Генерирање

```python
# Зачувај како: animations/YY_naziv.py
# Изврши:
manim -pql animations/YY_naziv.py MojaDemonstratcija
```

📖 **Детали**: `animations/README.md` (ако постои)

---

## 🖼️ Image Generation - За Илустрации

### Кога да користиш Image Gen?
- ✅ Декоративни илустрации
- ✅ Концептуални слики
- ✅ Нема строги математички барања

### Препорака
Користи DALL-E, Midjourney, или Stable Diffusion со prompt:
```
"Educational mathematics illustration, clean simple design, 
textbook style, [опис на задачата]"
```

Зачувај како: `media/images/grade_XX/YY_naziv.png`

---

## 📁 Структура на Проектот

```
mk-math-textbooks-visuals/
├── grade_06/          # 6то одделение
│   ├── 01_fractions_intro.py
│   └── media/images/
├── grade_07/
├── grade_08/
│   ├── 14_decimalni_broevi.py
│   ├── 15_decimalno_mnozenje.py
│   └── *.asy фајлови
├── grade_09/
├── grade_10/
├── grade_11/
├── grade_12/
├── animations/        # Manim анимации
├── static_diagrams/   # Asymptote тестови
└── media/
    ├── images/        # Генерирани слики
    ├── Tex/          # LaTeX изворни фајлови
    └── videos/       # Manim видеа
```

### Именување на фајлови

**Asymptote дијаграми:**
```
grade_08/14_decimalni_broevi_dijagram_01.asy
grade_08/15_decimalno_mnozenje_brojna_oska.asy
```

**Manim анимации:**
```
animations/08_decimalni_intro.py
animations/09_decimalno_mnozenje_demo.py
```

**Слики:**
```
media/images/grade_08/14_decimalni_ilustracija.png
```

---

## 🤖 AI Упатства - Како да генерираш код

### Чекор 1: Анализа на задачата

Прочитај ја задачата и идентификувај:
1. **Тип на визуелизација** (дијаграм, анимација, илустрација)
2. **Елементи** (форми, графици, текст, лабели)
3. **Барања** (големина, бои, стил)

### Чекор 2: Избери алатка

```
Дали треба анимација? → Manim
Дали е геометриски/математички дијаграм? → Asymptote
Дали е декоративна илустрација? → Image Gen
```

### Чекор 3: Генерирај код

**За Asymptote:**
1. Копирај го header-от (ЗАДОЛЖИТЕЛНО!)
2. Дефинирај големина: `size(400, 300);`
3. Цртај форми: `draw(...)`, `fill(...)`, `label(...)`
4. Додај македонски текст со `label("...")`

**За Manim:**
1. Наслови од `Scene`
2. Дефинирај објекти
3. Додај `.play()` анимации
4. Додај `.wait()` паузи

### Чекор 4: Зачувај и извести корисникот

```markdown
✅ Генериран код: grade_08/14_example.asy

За да го извршиш:
\`\`\`powershell
python render.py grade_08/14_example.asy
\`\`\`
```

---

## 📋 Примери - Типични Задачи

### Пример 1: "Нацртај координатен систем со точка A(3, 2)"

**AI одлука:** Asymptote (статички дијаграм)

**Генериран код:**
```asymptote
settings.tex="pdflatex";
settings.outformat="pdf";
texpreamble("\usepackage[T2A]{fontenc}");
texpreamble("\usepackage[utf8]{inputenc}");
texpreamble("\usepackage[russian]{babel}");
texpreamble("\usepackage{lmodern}");

size(400, 300);

// Оски
draw((-5,0)--(5,0), black+1.5, Arrow);
draw((0,-3)--(0,5), black+1.5, Arrow);

label("$x$", (5,0), E);
label("$y$", (0,5), N);
label("$O$", (0,0), SW);

// Точка A
dot((3,2), red+7);
label("$A(3,2)$", (3,2), NE, fontsize(11pt));
```

**Зачувај како:** `grade_08/koordinaten_sistem_A.asy`

---

### Пример 2: "Објасни како се собираат дропки - анимација"

**AI одлука:** Manim (анимација)

**Генериран код:**
```python
from manim import *

class SobiranjeDropki(Scene):
    def construct(self):
        # Прва дропка
        frac1 = MathTex(r"\frac{1}{4}")
        frac2 = MathTex(r"\frac{2}{4}")
        plus = MathTex("+")
        equals = MathTex("=")
        result = MathTex(r"\frac{3}{4}")
        
        # Аранжирај
        group = VGroup(frac1, plus, frac2, equals, result).arrange(RIGHT)
        
        # Анимација
        self.play(Write(frac1))
        self.play(Write(plus))
        self.play(Write(frac2))
        self.wait()
        self.play(Write(equals))
        self.play(Write(result))
        self.wait(2)
```

**Зачувај како:** `animations/08_sobiranje_dropki.py`

---

### Пример 3: "Илустрација на пица поделена на 8 делови"

**AI одлука:** Asymptote (може да биде дијаграм) или Image Gen (ако треба декоративно)

**Asymptote вариант:**
```asymptote
settings.tex="pdflatex";
settings.outformat="pdf";
texpreamble("\usepackage[T2A]{fontenc}");
texpreamble("\usepackage[utf8]{inputenc}");
texpreamble("\usepackage[russian]{babel}");
texpreamble("\usepackage{lmodern}");

size(300);

// Круг за пица
draw(circle((0,0), 2), black+1.5);
fill(circle((0,0), 2), lightgray+opacity(0.3));

// 8 делови (45 степени секој)
for (int i = 0; i < 8; ++i) {
    real angle = i * 45;
    draw((0,0)--(2*cos(radians(angle)), 2*sin(radians(angle))), black+1);
}

label("Пица поделена на 8 дела", (0, -2.5), fontsize(12pt));
```

---

## 🔧 Алатки за Процесирање

### render.py
Главна скрипта за рендерирање на Asymptote и Manim фајлови.

```powershell
python render.py grade_08/14_example.asy    # Asymptote
python render.py animations/08_demo.py      # Manim
```

### render_svg.py
Рендерира Asymptote → PDF → Отвара Illustrator (рачно Create Outlines → SVG)

```powershell
python render_svg.py grade_08/14_example.asy
```

### render_svg_auto.py
Обид за автоматизација (експериментално - бара подобрувања)

---

## 📚 Референтни Документи

- **ASYMPTOTE_AI_TEMPLATE.md** - Детален Asymptote темплејт со примери
- **FONT_SETUP_GUIDE.md** - Подесување на Latin Modern фонтови
- **SVG_EXPORT_GUIDE.md** - Водич за SVG експорт од Illustrator
- **LATEX_TEMPLATE_GUIDE.md** - LaTeX темплејт за учебници

---

## ⚡ Брза Референца за AI

### За статички дијаграм:
```
1. Користи Asymptote
2. Копирај header со кирилична поддршка
3. size(400, 300);
4. Цртај со draw(), fill(), label()
5. Зачувај како grade_XX/YY_naziv.asy
```

### За анимација:
```
1. Користи Manim
2. Наслови од Scene
3. Користи .play() и .wait()
4. Зачувај како animations/YY_naziv.py
```

### За илустрација:
```
1. Користи Image Gen
2. Prompt: "educational math illustration..."
3. Зачувај како media/images/grade_XX/YY_naziv.png
```

---

## 📝 MARKDOWN + LATEX OUTPUT (SimpleTex за Word)

AI треба ИСТО ТАКа да генерира **Markdown со LaTeX** за текстуалниот дел на задачите.  
Корисникот користи **SimpleTex add-in** во Microsoft Word за да го конвертира ова.

### Формат за SimpleTex:
```markdown
# Задача 1: Наслов на задачата

**Дадено:** Еднаквостите $x + 5 = 12$ и $2y - 3 = 7$.

**Барано:** Пресметај ја вредноста на $x + y$.

**Решение:**

Од првата еднаквост:
$$x + 5 = 12$$
$$x = 12 - 5 = 7$$

Од втората еднаквост:
$$2y - 3 = 7$$
$$2y = 10$$
$$y = 5$$

Конечен одговор:
$$x + y = 7 + 5 = 12$$

---
```

### LaTeX Math Синтакса:
- **Inline math**: `$x + 5$` за математика во текст
- **Display math**: `$$\frac{a}{b}$$` за центрирани формули
- **Дропки**: `\frac{numerator}{denominator}` → $\frac{3}{4}$
- **Грчки букви**: `\alpha`, `\beta`, `\pi` → $\alpha, \beta, \pi$
- **Оператори**: `\cdot` (·), `\times` (×), `\div` (÷)
- **Релации**: `\leq` (≤), `\geq` (≥), `\neq` (≠)
- **Корени**: `\sqrt{x}` → $\sqrt{x}$
- **Степени**: `x^2`, `x^{10}` → $x^2, x^{10}$
- **Индекси**: `x_1`, `x_{12}` → $x_1, x_{12}$

### Интеграција со SimpleTex:
1. Копирај AI-генериран Markdown
2. Во Word: **SimpleTex** → **Insert Markdown**
3. LaTeX формули автоматски се конвертираат во Word еднаквости
4. Кирилскиот текст се зачувува со форматирање

---

## 🎯 Финален Checklist за AI

Пред да го дадеш кодот на корисникот, провери:

- [ ] Дали е избрана вистинска алатка? (Asymptote/Manim/Image)
- [ ] Дали Asymptote има header за кирилични букви?
- [ ] Дали е кодот комплетен и извршлив?
- [ ] Дали е името на фајлот правилно? (grade_XX/YY_naziv)
- [ ] Дали си дал инструкции за извршување?
- [ ] **Дали си додал Markdown+LaTeX дел за SimpleTex?**

---

## 🚀 Употреба од AI Асистент

Кога корисникот ти даде задача:

```markdown
Корисник: "Нацртај триаголник АБВ со страни a=3, b=4, c=5"

AI Одговор:
✅ Одлучив: Asymptote (статички геометриски дијаграм)

📄 ASYMPTOTE КОД:
[тука комплетен код со header]

📝 MARKDOWN ЗА WORD (SimpleTex):
```markdown
# Задача: Триаголник АБВ

**Дадено:** Триаголник со страни $a=3$, $b=4$, $c=5$.

**Решение:** Проверка за правоаголен триаголник:
$$a^2 + b^2 = 3^2 + 4^2 = 9 + 16 = 25 = 5^2 = c^2$$

Заклучок: Триаголникот АБВ е правоаголен.
```

🚀 Извршување:
\`\`\`powershell
python render.py grade_08/triagolnik_ABV.asy
\`\`\`

💾 Зачувај како: grade_08/triagolnik_ABV.asy
```

---

**Последна ревизија:** 2026-01-02  
**Верзија:** 1.0  
**Статус:** 🟢 Production Ready
